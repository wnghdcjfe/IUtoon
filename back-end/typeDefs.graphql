scalar DateTime

type Song {
    title: String!
    url: String
    seeCount: String
    lyrics: String
    album: String
    date: DateTime 
    id: Int
    albumInfo: String
}

type Query{
    test: String!
    popularSong: [Song]
    titleSong(title:String): [Song]
    idSong(id:Int): [Song]
}